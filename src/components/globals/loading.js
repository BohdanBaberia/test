
import CircularProgress from '@mui/material/CircularProgress';

const Loading = () =>{

        return (
            <div style={{ width: '100%',textAlign: `center`, margin: `0 auto`, left: 0, right: 0 }}>
                <CircularProgress />
            </div>
        )
    }
    
export default Loading;